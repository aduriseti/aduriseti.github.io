source "https://rubygems.org"

# This gem includes the core GitHub Pages environment
gem "github-pages", group: :jekyll_plugins

# This allows your headers (# Heading) to become the post titles
gem "jekyll-titles-from-headings"